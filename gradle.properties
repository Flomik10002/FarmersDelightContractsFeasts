org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# Minecraft/Forge version - this branch targets 1.20.1 (the last version where Forge and NeoForge
# share the same net.minecraftforge.* package, so a single Forge build also runs on NeoForge 1.20.1
# unmodified - see docs/port_1.20.1_audit.md).
minecraft_version=1.20.1
forge_version=47.4.22
# Parchment mappings for 1.20.1 (matches what Farmer's Delight itself builds against)
parchment_minecraft_version=1.20.1
parchment_mappings_version=2023.09.03-1.20.1
mixin_extras_version=0.3.6
# Farmer's Delight version this mod is built against (Modrinth version number, Forge/NeoForge build)
farmers_delight_version=1.20.1-1.3.2
# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=farmerscontracts
# The human-readable display name for the mod.
mod_name=Farmer's Delight: Contracts & Feasts
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=All Rights Reserved
# The mod version. See https://semver.org/
mod_version=1.0.2
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=dev.flomik
# The authors of the mod. This is a simple text string that is used for display purposes in the mod list.
mod_authors=Flomik
# The description of the mod. This is a simple multiline text string that is used for display purposes in the mod list.
mod_description=Adds a Contract Board economy to Farmer's Delight: fulfill orders from village customers using its recipes for emeralds, XP and rare rewards.
